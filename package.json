{
  "name": "overlord",
  "version": "0.1.0",
  "private": true,
  "description": "Overlord cognitive orchestration runtime (MVP scaffold)",
  "type": "module",
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\"",
    "test:conformance": "node --test \"test/conformance/**/*.test.ts\"",
    "dev": "node src/index.ts"
  },
  "dependencies": {
    "smo-os": "git+https://github.com/colombmax-cmd/Smo.OS.git"
  }
}
